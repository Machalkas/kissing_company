server:
  address: 0.0.0.0
#  port: 3233
  port: 33770
  error:
    include-message: ALWAYS


springdoc:
  swagger-ui:
    disable-swagger-default-url: true

spring:
  servlet:
    multipart:
      max-file-size: 30MB
  datasource:
#    url: jdbc:postgresql://localhost:5432/kissing_company
    url: jdbc:postgresql://192.168.10.80:5432/studs
#    username: admin
#    password: admin
    username: s337706
    password: KbnSjgwLDV2n9vw3
    driver-class-name: org.postgresql.Driver
    data: init.sql
    initialization-mode: always
  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate.format_sql: true
      hibernate.jdbc.lob.non_contextual_creation: true
      hibernate:
#        default_schema: public
        default_schema: s337706
  liquibase:
    change-log: classpath:/db/changelog/changelog.yml
    enabled: false
  web:
    resources:
      static-locations: classpath:/static

security:
  config: com.mpi.kissing_company.security.CustomSecurityConfiguration

logging:
  level:
    org:
      springframework:
        security: DEBUG

static_path:
  girls_images: null
